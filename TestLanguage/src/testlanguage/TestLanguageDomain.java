package testlanguage;

import tara.magritte.Model;

public class TestLanguageDomain extends testlanguage.ModelWrapper implements tara.magritte.Domain {

	public TestLanguageDomain(Model model) {
		super(model);
		// Insert code here
	}
}