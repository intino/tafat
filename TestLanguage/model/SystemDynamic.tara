dsl Tafat 

Entity System

Behavior Evolver on System
	EquationSystem eq
		var double potentialAdopters
		Flow('$.potentialAdopters()') adoptionFromAd
		Flow('$.potentialAdopters() + $.adopters().value()') adoptionFromWOM
		Flow('return $.adoptionFromAd().value() + $.adoptionFromWOM().value();') adoptionRate
		Stock('$.adoptionRate().value()') adopters
