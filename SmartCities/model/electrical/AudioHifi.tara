dsl Tafat
use Residential

Behavior Electrical &000.001
	on AudioHifi
		var measure:Power activePower = 0 W
		var measure:Power standbyPower = 8.2 W
		var measure:Power installedPower = 14 W
		var word mode > Off*; StandBy; On
		Condition > Check(is_off); Action(power_0)
		Condition > Check(is_stand_by); Action(power_stand_by)
		Condition > Check(is_on); Action(power_installed_power)
