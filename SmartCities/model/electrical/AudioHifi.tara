dsl Tafat
use Residential

Behavior Electrical &000.001
	on AudioHifi
		var measure:Power activePower = 0 W
		var measure:Power standbyPower = 8.2 W
		var measure:Power installedPower = 14 W
		var word mode > Off*; StandBy; On
		ConditionalAction("() -> mode() == Mode.Off", "() -> activePower(0)")
		ConditionalAction("() -> mode() == Mode.StandBy", "() -> activePower(standByPower())")
		ConditionalAction("() -> mode() == Mode.On", "() -> activePower(installedPower())")
