dsl Tafat
use Residential

Behavior Electrical &000.001
	on AudioHifi
		var measure:Power activePower = 0 W
		var measure:Power standbyPower = 8.2 W
		var measure:Power installedPower = 14 W
		var word mode > Off*; StandBy; On
		Condition > Check("mode() == Mode.Off"); Action("activePower(0)")
		Condition > Check("mode() == Mode.StandBy"); Action("activePower(standByPower())")
		Condition > Check("mode() == Mode.On"); Action("activePower(installedPower())")
