dsl Tafat
use Residential

Behavior Electrical &000.001
	on Computer
		var measure:Power activePower = 0 W
		var measure:Power standByPower = 7.1 W
		var measure:Power intalledPower = 100 W
		var word mode > Off*; StandBy; On
		Condition > Check("mode() == Mode.Off"); Action("power(0)")
		Condition > Check("mode() == Mode.StandBy"); Action("power(StandBy)")
		Condition > Check("mode() == Mode.On"); Action("power(installedPower)")
