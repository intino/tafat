import org.junit.Test;
import tafat.engine.EventManager;
import tafat.engine.events.SingleEvent;

public class TimeoutTest {

    @Test
    public void testName() throws Exception {
//        final int[] array = {0};
//        EventManager.register(new SingleEvent(3, () -> array[0] = 4));
    }
}
